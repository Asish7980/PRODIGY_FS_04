# Backend server port
PORT=5001

# MongoDB connection string (replace with your actual MongoDB URI)
MONGO_URI=mongodb://localhost:27017/chat-app

# Or if using MongoDB Atlas, something like:
# MONGO_URI=mongodb+srv://yourUser:yourPassword@cluster0.mongodb.net/chat-app?retryWrites=true&w=majority

# Secret key for JWT token signing (choose a strong random string)
JWT_SECRET=your_jwt_secret_key_here
